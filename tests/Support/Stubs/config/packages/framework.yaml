framework:
    secret: '%env(APP_SECRET)%'
    default_locale: en
    http_method_override: true

    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file

    router:
        utf8: true
